# StockPricePredictor

Created a recurrent neural network (RNN) called Long Term Short Memory (LTSM) to predict the closing stock price using the stock prices in the last 60 days.

Used Jupyter Notebook so it is easier to look at the data.
